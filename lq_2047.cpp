#include <iostream>
using namespace std;

const int N = 1e6+5;
char s[N], p[N];
//j for p, i for s
int nex[N]; // pre=suffix

int main(){
    cin >> p+1;
    int m = strlen(p+1);
    cin >> s+1;
    int n = strlen(s+1);
    nex[0] = 0;
    nex[1] = 0;
    for (int i = 2, j = 0; i <= m; i++){

    }
}
